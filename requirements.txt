requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
python-dotenv>=1.0.0

# python-dotenv is already listed above — it loads .env files automatically
# in local development so you don't need to export env vars manually.
